# "The Hunter's Path" - Игра в стиле Darkest Dungeon

## Введение

### Цель проекта

"The Hunter's Path" - это курсовой проект по дисциплине объектно-ориентированного программирования (ООП), разработанный студентом БГУИР. Проект представляет собой пошаговую RPG-игру в стиле Darkest Dungeon с элементами rogue-like, где игрок управляет отрядом героев, сражающихся с силами тьмы.

### Назначение

Игра демонстрирует применение принципов объектно-ориентированного программирования в разработке сложной игровой системы. Проект включает:

- Модульную архитектуру с разделением ответственности
- Иерархию классов с наследованием и полиморфизмом
- Паттерны проектирования (Factory, Singleton, Observer)
- Систему боя с тактическими элементами
- Генерацию контента и нелинейный геймплей

### Контекст курсового проекта

Проект разработан на языке C++ с использованием библиотеки SFML для графического интерфейса. Он демонстрирует глубокое понимание ООП принципов через реализацию комплексной игровой логики, включая управление состоянием, событийную систему и взаимодействие между компонентами.

## Архитектура проекта

### Общая структура

Проект построен по модульной архитектуре с четким разделением ответственности между компонентами:

```
src/
├── entities/           # Игровые сущности
│   ├── Entity.h        # Базовый класс сущности
│   ├── Player.h        # Класс игрока
│   └── Enemy.h         # Классы врагов
├── systems/            # Игровые системы
│   ├── BattleSystem.h  # Система боя
│   ├── CampaignSystem.h # Система кампании
│   ├── Map.h           # Система карты
│   └── GUI.h           # Графический интерфейс
├── factories/          # Фабрики объектов
│   ├── HeroFactory.h   # Фабрика героев
│   ├── EnemyFactory.h  # Фабрика врагов
│   └── ItemFactory.h   # Фабрика предметов
└── data/              # Данные игры
    ├── HeroTemplates.h # Шаблоны героев
    ├── EnemyTemplates.h # Шаблоны врагов
    └── ItemTemplates.h # Шаблоны предметов
```

### Паттерны проектирования

Проект использует следующие паттерны:

1. **Factory Pattern** - Для создания героев, врагов и предметов
2. **Singleton Pattern** - Для управления глобальными ресурсами
3. **Observer Pattern** - Для обработки событий в системе боя
4. **Strategy Pattern** - Для различных типов способностей
5. **Component Pattern** - Для модульной структуры сущностей

### Принципы ООП

#### Инкапсуляция

Все данные классов скрыты за приватными полями, доступ осуществляется через геттеры и сеттеры:

```cpp
class Entity {
private:
    string m_name;
    int m_max_healthpoint;
    int m_current_healthpoint;
    // ... другие поля

public:
    string getName() const { return m_name; }
    void setCurrentHealthPoint(int hp) {
        if (hp >= 0 && hp <= m_max_healthpoint) {
            m_current_healthpoint = hp;
        } else {
            throw invalid_argument("Неверное значение HP");
        }
    }
};
```

#### Наследование

Иерархия классов строится от базового класса Entity:

```
Entity (абстрактный базовый класс)
├── Player (игрок с инвентарем и уровнем)
└── Enemy (враг с опытом)
    ├── Goblin (быстрый ядовитый враг)
    ├── Orc (сильный медленный берсерк)
    ├── Vampire (вампир с вампиризмом)
    ├── Wyvern (летающий враг)
    ├── Ghost (призрак с невидимостью)
    └── Troglodyte (троглодит с регенерацией)
```

#### Полиморфизм

Полиморфизм реализуется через виртуальные методы:

```cpp
class Enemy : public Entity {
public:
    virtual void useAbility(Entity &target) {
        // Базовая реализация
        cout << getName() << " не имеет специальной способности.\n";
    }
};

class Goblin : public Enemy {
public:
    void useAbility(Entity &target) override {
        // Специфическая реализация для гоблина
        cout << getName() << " использует Яд на " << target.getName() << "!\n";
        target.takeDamage(5);
    }
};
```

#### Абстракция

Абстракция достигается через интерфейсы и абстрактные классы. Например, система эффектов абстрагирует различные типы воздействий:

```cpp
enum class EffectType {
    BUFF_DAMAGE,    // Увеличение урона
    DEBUFF_DAMAGE,  // Уменьшение урона
    POISON_DAMAGE,  // Яд
    REGENERATION,   // Регенерация
    STEALTH,        // Невидимость
    INVISIBLE       // Прозрачность
};

struct Effect {
    EffectType type;
    int value;
    int duration;
    string name;
};
```

### Взаимодействие компонентов

Компоненты взаимодействуют через четко определенные интерфейсы:

- **BattleSystem** управляет боем, используя Entity объекты
- **CampaignSystem** координирует общую игру, используя Map и BattleSystem
- **GUI** отображает состояние, получая данные от CampaignSystem
- **Factory** классы создают объекты по запросу других систем

### Модули и зависимости

Проект разделен на независимые модули с минимальными зависимостями:

- `entity.h/cpp` - независимый модуль сущностей
- `BattleSystem.h/cpp` - зависит только от entity
- `CampaignSystem.h/cpp` - зависит от BattleSystem, Map, factories
- `GUI.h/cpp` - зависит от SFML и основных систем

## Подробное описание классов

### Класс Entity (entity.h)

Базовый класс для всех игровых сущностей.

#### Поля

- `string m_name` - имя сущности
- `AbilityType m_ability` - базовая способность
- `int m_max_healthpoint` - максимальное здоровье
- `int m_current_healthpoint` - текущее здоровье
- `int m_damage` - базовый урон
- `int m_defense` - защита
- `int m_attack` - точность атаки
- `int m_max_stamina` - максимальная выносливость
- `int m_current_stamina` - текущая выносливость
- `int m_initiative` - инициатива (определяет порядок ходов)
- `int m_attack_range` - дальность атаки (0-ближний, 1-средний, 2-дальний)
- `double m_damage_variance` - разброс урона (0.0-1.0)
- `vector<Effect> m_activeEffects` - активные эффекты

#### Методы

##### Конструктор

```cpp
Entity(const string &name = "Entity", int max_hp = 100, int damage = 10,
       int defense = 0, int attack = 0, int max_stamina = 1,
       int c_stamina = 1, int initiative = 10, int attack_range = 0,
       AbilityType ability = AbilityType::NONE, double damage_variance = 0.2)
```

Создает сущность с заданными параметрами. Это как рождение персонажа в игре - мы даем ему имя, здоровье, силу и другие характеристики, как будто лепим глиняную фигурку и вдыхаем в нее жизнь.

##### Геттеры и сеттеры

- `getName()`, `setName(string)` - получают и устанавливают имя, как ярлык на чемодане
- `getMaxHealthPoint()`, `setMaxHealthPoint(int)` - управляют максимальным здоровьем, как размер бака для бензина в машине
- `getCurrentHealthPoint()`, `setCurrentHealthPoint(int)` - текущее здоровье, как уровень топлива в баке
- Аналогично для damage, defense, attack, stamina, initiative, attack_range

##### Методы эффектов

```cpp
void addEffect(const Effect &effect)
```

Добавляет эффект к сущности. Представьте, что это как наклеить магнит на холодильник - эффект "прилипает" и начинает действовать.

```cpp
void updateEffects()
```

Обновляет все активные эффекты каждый ход. Это как часы в игре - тикают, и эффекты либо продолжают действовать, либо истекают, как песок в песочных часах.

```cpp
void applyEffect(const Effect &effect)
```

Применяет конкретный эффект. Например, для BUFF_DAMAGE увеличивает урон, как будто надеваете волшебные перчатки, которые делают удары сильнее.

##### Методы действий

```cpp
int attack(int recipient_protection)
```

Рассчитывает урон атаки с учетом защиты противника. Это как бросить мяч в баскетбольное кольцо - учитывается защита соперника, и мяч может не долететь или отскочить.

```cpp
void heal(int heal_amount)
```

Восстанавливает здоровье. Как выпить лекарство - здоровье возвращается, но не больше максимума.

```cpp
void takeDamage(int received_damage)
```

Получает урон. Как удар молотком по стеклу - здоровье уменьшается, и если становится 0, сущность "разбивается".

##### Вспомогательные методы

```cpp
string getHealthBarString() const
```

Возвращает строку с прогресс-баром здоровья, как индикатор заряда батареи на телефоне.

### Класс Player (наследуется от Entity)

Расширяет Entity возможностями игрока.

#### Дополнительные поля

- `int m_level` - уровень персонажа
- `int m_required_experience` - опыт для следующего уровня
- `int m_received_experience` - текущий опыт
- `HeroClass m_hero_class` - класс героя (WARRIOR, MAGE и т.д.)
- `ProgressionType m_progression_type` - тип прогрессии
- `vector<AbilityType> m_available_abilities` - доступные способности
- `map<AbilityType, int> m_ability_levels` - уровни способностей
- `bool m_is_loner` - флаг одиночки
- `vector<Item> m_inventory` - инвентарь
- `map<EquipmentSlot, Item> m_equipment` - экипировка

#### Методы

##### Управление инвентарем

```cpp
void addItem(const Item &item)
```

Добавляет предмет в инвентарь. Как положить вещь в рюкзак - она занимает место и становится доступной для использования.

```cpp
void equipItem(int inventoryIndex)
```

Экипирует предмет из инвентаря. Как надеть куртку - предмет перемещается из рюкзака на тело и дает бонусы.

```cpp
void unequipItem(EquipmentSlot slot)
```

Снимает предмет с экипировки. Как снять куртку - предмет возвращается в рюкзак, бонусы пропадают.

##### Прогрессия

```cpp
void upLevel()
```

Повышает уровень при достаточном опыте. Как вырасти из старой одежды - характеристики улучшаются, открываются новые возможности.

##### Отображение

```cpp
string getExperienceBarString() const
```

Показывает прогресс опыта, как шкала загрузки в игре.

### Классы врагов (наследуются от Enemy, который наследуется от Entity)

#### Goblin

Быстрый слабый враг со способностью Poison.

```cpp
void useAbility(Entity &target) override
```

Использует яд: наносит урон и может отравить цель. Как укус змеи - быстрый укус, а потом жертва чувствует жжение.

#### Orc

Сильный медленный берсерк.

```cpp
void useAbility(Entity &target) override
```

Входит в берсерк: увеличивает урон, уменьшает защиту. Как разъяренный бык - становится сильнее, но уязвимее.

#### Vampire

Вампир с вампиризмом.

```cpp
void useAbility(Entity &target) override
```

Крадет жизнь: наносит урон и лечится. Как вампир в фильме - кусает и пьет кровь, становясь сильнее.

### Класс BattleSystem

Управляет логикой боя.

#### Основные методы

```cpp
void startBattle(const vector<Entity *> &players, const vector<Entity *> &enemies)
```

Начинает бой между игроками и врагами. Как начало футбольного матча - команды выходят на поле, и начинается игра.

```cpp
bool attack(Entity *attacker, Entity *target)
```

Выполняет атаку. Как удар в боксе - рассчитывается урон, применяется, проверяется победа.

```cpp
void nextTurn()
```

Переходит к следующему ходу. Как смена ходов в шахматах - один игрок закончил, теперь ход другого.

#### Вспомогательные методы

```cpp
bool canAttackTarget(Entity *attacker, Entity *target) const
```

Проверяет возможность атаки. Как проверка, можно ли ударить по мячу в теннисе - зависит от позиции и дальности.

```cpp
vector<pair<Entity *, int>> getAvailableTargetsForCurrent() const
```

Возвращает доступные цели для текущего атакующего. Как показать возможные ходы в шахматах.

### Класс CampaignSystem

Управляет общей кампанией игры.

#### Основные методы

```cpp
void startCampaign()
```

Начинает новую кампанию. Как начало путешествия - выбираем героев, готовимся к приключениям.

```cpp
void runCampaignLoop()
```

Основной цикл кампании. Как путешествие по дороге - события происходят одно за другим.

```cpp
void handleNodeEvent(NodeType nodeType)
```

Обрабатывает событие узла карты. Как открыть дверь в доме - за ней может быть сокровище или монстр.

### Фабричные классы

#### HeroFactory

Создает героев по шаблонам.

```cpp
Player *createHero(HeroClass heroClass, const string &customName = "")
```

Создает героя выбранного класса. Как выбрать персонажа в игре - указываешь класс, и фабрика "собирает" героя со всеми нужными характеристиками.

#### ItemFactory

Создает предметы.

```cpp
Item createRandomItem()
```

Создает случайный предмет. Как открыть сундук - никогда не знаешь, что там будет.

## Принципы SOLID в проекте

### Single Responsibility Principle (SRP)

Каждый класс отвечает только за одну задачу:

- `BattleSystem` - только за бой
- `CampaignSystem` - только за кампанию
- `HeroFactory` - только за создание героев

### Open/Closed Principle (OCP)

Классы открыты для расширения, но закрыты для модификации:

- Новые типы врагов добавляются наследованием от `Enemy`
- Новые способности добавляются через `AbilityType` enum

### Liskov Substitution Principle (LSP)

Объекты подклассов могут заменять объекты базового класса:

- `Player` и `Enemy` могут использоваться как `Entity`
- Все враги могут участвовать в бою через общий интерфейс

### Interface Segregation Principle (ISP)

Интерфейсы разделены на специфические:

- `Entity` предоставляет только базовые методы
- Специфические классы добавляют свои интерфейсы

### Dependency Inversion Principle (DIP)

Высокоуровневые модули не зависят от низкоуровневых:

- `CampaignSystem` зависит от абстракций (`BattleSystem`, `Map`), а не конкретных реализаций

## Паттерны проектирования

### Factory Pattern

Используется для создания сложных объектов:

```cpp
class HeroFactory {
public:
    static Player *createHero(HeroClass heroClass) {
        const HeroTemplate &tmpl = heroTemplates[heroClass];
        return new Player(/* параметры из шаблона */);
    }
};
```

### Singleton Pattern (неявно)

Фабрики используют статические методы и данные, обеспечивая единственный источник создания объектов.

### Observer Pattern

Система эффектов наблюдает за изменениями состояния сущностей.

### Strategy Pattern

Разные способности реализуют разные стратегии поведения:

```cpp
switch (ability) {
    case AbilityType::BERSERK:
        // Стратегия берсерка
        break;
    case AbilityType::HEAL:
        // Стратегия лечения
        break;
}
```

## Установка и запуск проекта

### Системные требования

- Windows 10/11
- Visual Studio 2019/2022 с поддержкой C++17
- SFML 2.6.1

### Установка зависимостей

1. Скачайте SFML 2.6.1 для Windows
2. Распакуйте в папку `SFML-2.6.1` в корне проекта
3. Убедитесь, что пути в `CppProperties.json` корректны

### Сборка проекта

1. Откройте `UNI.CourseWork.Private.Game 'The Hunter's Path'.sln` в Visual Studio
2. Выберите конфигурацию Release
3. Соберите проект (Build → Build Solution)

### Запуск игры

1. Запустите `game.exe` из папки сборки
2. Выберите пресет партии в главном меню
3. Наслаждайтесь игрой!

## Походовый режим (Campaign Mode)

Походовый режим - это сердце игры "The Hunter's Path", где игрок исследует мир, сражается с врагами и собирает сокровища. Это как путешествие по волшебной стране, где каждый шаг может привести к приключению или опасности.

### Архитектура походового режима

Походовый режим управляется классом `CampaignSystem`, который координирует:

- **Локации** - четыре уникальные зоны (Forest, Cave, Dead City, Castle)
- **Карту** - процедурно генерируемую сетку 15x15 с узлами событий
- **События** - бои, сокровища, текстовые события и переходы
- **Партию** - управление отрядом героев и инвентарем

### Локации мира

Игра разделена на четыре локации, каждая со своими особенностями:

#### 🌲 Forest (Лес)

- **Описание**: Густой лес, полный опасных существ и древних секретов
- **Особенности**: Высокая вероятность боев с дикими животными, сокровища в виде эльфийских артефактов
- **Соединения**: Cave, Dead City

#### 🕳️ Cave (Пещеры)

- **Описание**: Глубокие подземные пещеры, населенные троглодитами и элементалями
- **Особенности**: Темные туннели с засады гоблинов, ценные кристаллы и руды
- **Соединения**: Forest, Dead City

#### 🏛️ Dead City (Мертвый город)

- **Описание**: Заброшенный город, населенный нежитью и призраками
- **Особенности**: Разрушенные здания с сокровищами древних правителей, атаки скелетов и вампиров
- **Соединения**: Cave, Castle

#### 🏰 Castle (Замок)

- **Описание**: Древний замок, где обитает финальный босс и его приспешники
- **Особенности**: Только финальный бой с Lord of Darkness
- **Соединения**: Нет (финальная локация)

### Система карты

Карта представляет собой сетку 15x15 клеток, где каждая клетка - это узел с определенным типом события:

#### Типы узлов

- **I (START)** - Точка входа в локацию
- **B (BATTLE)** - Бой с врагами
- **S (EVENT)** - Текстовое событие с выбором
- **P (TREASURE)** - Сокровище или предмет
- **V (EXIT)** - Выход из локации
- **F/C/D/Z** - Специальные узлы локаций (Forest/Cave/Dead City/Castle)
- **. (EMPTY)** - Пустая клетка
- **? (UNKNOWN)** - Неисследованная область

#### Генерация карты

```cpp
void Map::generateFixedMap()
{
    // Использует прототипы из mapPrototypesData.h
    // Создает коридоры и размещает специальные узлы
    // Гарантирует связность карты
}
```

Карта генерируется на основе фиксированных прототипов для каждой локации, обеспечивая интересный геймплей при сохранении предсказуемости.

#### Движение по карте

Игрок перемещается по карте с помощью клавиш WASD:

```cpp
bool Map::movePlayer(char direction)
{
    // w - вверх, a - влево, s - вниз, d - вправо
    // Проверяет валидность хода
    // Обновляет позицию игрока
}
```

### Система событий

Каждый узел карты может вызвать событие. События обрабатываются в `handleNodeEvent()`:

#### Боевые события (BATTLE)

- Генерируют 1-4 врагов в зависимости от сложности
- Враги создаются фабрикой `EnemyFactory` на основе типа локации
- После победы дают опыт и могут оставить предметы

#### События сокровищ (TREASURE)

- Дают случайные предметы из `ItemFactory`
- Предметы могут быть оружием, броней, аксессуарами или расходниками

#### Текстовые события (EVENT)

- Предлагают выбор действий
- Выбор влияет на исход (бой или сокровище)

#### События выхода (EXIT)

- Позволяют перейти в следующую локацию
- Восстанавливают здоровье всей партии
- Увеличивают сложность игры

### Специальные узлы локаций

Каждая локация имеет уникальные узлы с атмосферными событиями:

#### Forest (F)

- **Бои**: "Шорох листьев предвещает опасность - дикие звери леса вышли на охоту!"
- **Сокровища**: "Среди корней древнего дуба вы находите эльфийский амулет, мерцающий в лунном свете"

#### Cave (C)

- **Бои**: "Шаги эхом отдаются из пещерной тьмы - троглодиты и их питомцы атакуют!"
- **Сокровища**: "В кристальной пещере вы находите драгоценный камень, пульсирующий внутренней энергией"

#### Dead City (D)

- **Бои**: "Призрачные силуэты материализуются - мертвые жители города восстают!"
- **Сокровища**: "В руинах дворца вы находите королевскую корону, хранящую эхо былой славы"

#### Castle (Z)

- **Финальный бой**: "Вы проникли в сердце замка и столкнулись с Lord of Darkness!"

### Переходы между локациями

При достижении выхода игрок может выбрать следующую локацию:

```cpp
void CampaignSystem::handleExitChoice(int choiceIndex)
{
    // Изменяет currentLocation
    // Генерирует новую карту
    // Очищает visitedNodes
    // Восстанавливает здоровье партии
    // Увеличивает currentDifficulty
}
```

Переход в новую локацию:

- Полностью восстанавливает здоровье всех героев
- Генерирует новую карту
- Сбрасывает посещенные узлы
- Увеличивает глобальную сложность

### Система сложности

Сложность игры растет с каждым событием:

- `currentDifficulty` увеличивается после каждого события
- Влияет на количество и силу врагов
- Определяет качество выпадающих предметов

### Управление партией

В походовом режиме игрок может:

- **Исследовать карту** - двигаться WASD
- **Сражаться** - автоматический переход в боевую систему
- **Собирать сокровища** - добавление в инвентарь
- **Управлять инвентарем** - экипировка, использование предметов
- **Переходить между локациями** - выбор следующей зоны

### Примеры игрового процесса

#### Начало игры

1. Выбор пресета партии (одиночка или группа)
2. Появление в Forest у точки входа (I)
3. Исследование карты, обнаружение боев и сокровищ

#### Типичное исследование

```
Карта показывает: I . . B . S
Игрок идет:     @ . . B . S
Доступные ходы: d (вправо)
```

#### Переход в новую локацию

```
Выход найден! Доступные локации:
1. Cave - Глубокие подземные пещеры...
2. Dead City - Заброшенный город...

Выберите локацию для перехода (1-2):
```

Походовый режим сочетает исследование, тактические бои и постепенное продвижение через все более опасные локации к финальному противостоянию с Lord of Darkness.

## Основной алгоритм программы

"The Hunter's Path" построена на основе конечного автомата состояний, где основная логика реализована в главном цикле `main()` функции. Программа представляет собой событийно-ориентированное приложение с использованием библиотеки SFML для графики и ввода.

### Архитектура главного цикла

```cpp
int main() {
    // 1. ИНИЦИАЛИЗАЦИЯ
    Инициализация окна SFML
    Загрузка шрифтов
    Создание меню и интерфейсов
    Инициализация системы кампании

    // 2. ГЛАВНЫЙ ЦИКЛ ПРОГРАММЫ
    while (window.isOpen()) {
        // 2.1 ОБРАБОТКА СОБЫТИЙ
        Обработка событий SFML (клавиатура, мышь, закрытие окна)

        // 2.2 ОБНОВЛЕНИЕ СОСТОЯНИЯ
        switch (currentState) {
            case MAIN_MENU:         // Главное меню
            case CREDITS:           // Экран кредитов
            case CHARACTER_SELECTION: // Выбор персонажей
            case CHARACTER_CONFIRMATION: // Подтверждение выбора
            case CAMPAIGN:          // Инициализация кампании
            case MAP_MODE:          // Походовый режим
            case TREASURE:          // Окно сокровищ
            case EVENT:             // Окно событий
            case EXIT:              // Окно выхода
            case BATTLE:            // Боевой режим
            case INVENTORY:         // Инвентарь
            case VICTORY:           // Экран победы
            case DEFEAT:            // Экран поражения
        }

        // 2.3 ОТРИСОВКА
        Очистка окна
        Отрисовка текущего состояния
        Отображение на экране
    }

    return 0;
}
```

### Диаграмма состояний программы

```
MAIN_MENU ──────────────► CREDITS
    │                        │
    │                        ▼
    ▼                   MAIN_MENU
CHARACTER_SELECTION
    │
    ▼
CHARACTER_CONFIRMATION
    │
    ▼
CAMPAIGN ───► MAP_MODE ◄─────────────────┐
             │         │                  │
             │         ▼                  │
             │     TREASURE               │
             │         │                  │
             │         ▼                  │
             │      EVENT                 │
             │         │                  │
             │         ▼                  │
             │      EXIT                  │
             │         │                  │
             │         ▼                  │
             └────► BATTLE ◄──────────────┘
                     │
                     ▼
                 VICTORY/DEFEAT ──► MAIN_MENU
```

### Подробное описание состояний

#### 1. MAIN_MENU (Главное меню)

**Назначение**: Стартовый экран с основными опциями игры
**Действия**:

- Отображение заголовка и описания игры
- Кнопки: "Start Game", "Credits", "Exit"
- Переход в CHARACTER_SELECTION при выборе "Start Game"

#### 2. CREDITS (Кредиты)

**Назначение**: Экран с информацией о разработчике
**Действия**:

- Отображение текста "Mikulski Stanislau - BSUIR student"
- Кнопка "Back" для возврата в главное меню

#### 3. CHARACTER_SELECTION (Выбор персонажей)

**Назначение**: Выбор готового набора персонажей
**Действия**:

- Отображение списка доступных пресетов партий
- Для каждого пресета: название, описание, состав героев
- Переход в CHARACTER_CONFIRMATION при выборе

#### 4. CHARACTER_CONFIRMATION (Подтверждение выбора)

**Назначение**: Детальный просмотр выбранной партии
**Действия**:

- Отображение подробной информации о каждом герое
- Характеристики, способности, классы
- Кнопки "Confirm" (создание партии) и "Back"

#### 5. CAMPAIGN (Инициализация кампании)

**Назначение**: Переходное состояние для настройки кампании
**Действия**:

- Создание партии героев из выбранного пресета
- Инициализация карты и локаций
- Автоматический переход в MAP_MODE

#### 6. MAP_MODE (Походовый режим)

**Назначение**: Основной режим исследования и передвижения
**Действия**:

- Отображение ASCII-карты 15x15
- Информация о текущей локации и сложности
- Статус партии (уровни, опыт)
- Обработка клавиш WASD для движения
- Переход в другие состояния при наступлении на специальные узлы

#### 7. TREASURE (Сокровища)

**Назначение**: Обработка найденных предметов
**Действия**:

- Отображение найденного предмета
- Кнопки "Take" (взять) и "Decline" (отказаться)
- Возврат в MAP_MODE

#### 8. EVENT (События)

**Назначение**: Текстовые события с выбором
**Действия**:

- Отображение описания события
- Кнопки выбора действий
- Переход в бой или получение сокровищ

#### 9. EXIT (Выход из локации)

**Назначение**: Переход между локациями
**Действия**:

- Отображение доступных локаций для перехода
- Выбор следующей зоны
- Восстановление здоровья партии

#### 10. BATTLE (Бой)

**Назначение**: Тактические пошаговые сражения
**Действия**:

- Отображение поля боя и статуса сущностей
- Управление героями (атака, способности, перемещение)
- Автоматическое управление врагами
- Переход в VICTORY или DEFEAT

#### 11. INVENTORY (Инвентарь)

**Назначение**: Управление предметами и экипировкой
**Действия**:

- Выбор героя для управления
- Просмотр характеристик и экипировки
- Экипировка/снятие предметов
- Использование расходуемых предметов

#### 12. VICTORY/DEFEAT (Победа/Поражение)

**Назначение**: Финальные экраны
**Действия**:

- Отображение результата игры
- Кнопка возврата в главное меню

### Алгоритм обработки событий

```cpp
// В главном цикле
while (window.pollEvent(event)) {
    if (event.type == Event::Closed) {
        window.close();
    }

    // Обработка в зависимости от состояния
    switch (currentState) {
        case GameState::MAIN_MENU:
            mainMenu.handleEvent(event);
            break;
        case GameState::MAP_MODE:
            // Обработка клавиш движения WASD
            if (event.type == Event::KeyPressed) {
                char move = getMoveFromKey(event.key.code);
                if (campaign.movePlayer(move)) {
                    // Проверка наступления на событие
                    handleNodeEvent();
                }
            }
            break;
        case GameState::BATTLE:
            // Обработка боевых команд
            handleBattleInput(event);
            break;
        // ... другие состояния
    }
}
```

### Алгоритм обновления состояний

```cpp
// После обработки событий
switch (currentState) {
    case GameState::CHARACTER_CONFIRMATION:
        // Динамическое создание меню подтверждения
        updateCharacterConfirmationMenu();
        break;
    case GameState::TREASURE:
        // Обновление меню сокровищ
        updateTreasureMenu();
        break;
    case GameState::BATTLE:
        // Обновление состояния боя
        updateBattleState();
        break;
    // ... другие состояния
}
```

### Алгоритм отрисовки

```cpp
// В конце главного цикла
window.clear(Color::Black);

switch (currentState) {
    case GameState::MAIN_MENU:
        window.draw(title);
        window.draw(subtitle);
        mainMenu.draw();
        break;
    case GameState::MAP_MODE:
        drawMap();
        drawLocationInfo();
        drawPartyStatus();
        drawInventoryButton();
        break;
    case GameState::BATTLE:
        drawBattlefield();
        battleMenu.draw();
        break;
    // ... другие состояния
}

window.display();
```

### Управление памятью и ресурсами

- **SFML объекты**: Создаются один раз при инициализации
- **Меню и интерфейсы**: Переиспользуются между состояниями
- **Игровые объекты**: Создаются в фабриках, уничтожаются при завершении
- **Шрифты**: Загружаются при старте, используются во всей программе

### Обработка ошибок

- **Загрузка шрифтов**: Fallback на системные шрифты
- **Создание окна**: Проверка доступности полноэкранного режима
- **Игровая логика**: Валидация входных данных и состояний

Этот алгоритм обеспечивает четкое разделение ответственности между состояниями игры, позволяя легко добавлять новые режимы и модифицировать существующие без нарушения общей структуры программы.

## Примеры использования

### Создание героя

```cpp
// Создание воина через фабрику
Player *warrior = HeroFactory::createHero(HeroClass::WARRIOR, "Arthur");
cout << warrior->getName() << " создан!" << endl;
```

### Бой между сущностями

```cpp
// Создание бойцов
Entity *player = new Player("Hero", 100, 15, 5, 3);
Entity *enemy = new Enemy("Goblin", 50, 8, 2, 1);

// Атака
int damage = player->attack(enemy->getDefense());
enemy->takeDamage(damage);
cout << "Герой наносит " << damage << " урона!" << endl;
```

### Использование способностей

```cpp
// Герой использует лечение
Player *paladin = HeroFactory::createHero(HeroClass::PALADIN);
paladin->useAbility(AbilityType::HEALING_WAVE); // Лечит всех союзников
```

## Тестирование

Проект включает модульные тесты для ключевых компонентов:

### Тесты боя

- `test_enemy_deletion.cpp` - проверка удаления мертвых врагов
- `test_progressbars.cpp` - проверка отображения здоровья
- `test_items.cpp` - проверка работы предметов
- `test_abilities.cpp` - проверка способностей
- `test_shadow_step.cpp` - проверка способности Shadow Step
- `test_inventory.cpp` - проверка инвентаря

### Запуск тестов

```bash
# Собрать и запустить тесты
msbuild /p:Configuration=Debug
# Запустить конкретный тест
./test_enemy_deletion.exe
```

## Заключение

"The Hunter's Path" демонстрирует комплексное применение объектно-ориентированного программирования в разработке игры. Проект показывает, как принципы ООП и паттерны проектирования позволяют создавать поддерживаемый, расширяемый код.

После написания README нужно закоммитить изменения и запушить их в репозиторий.
